# Where waitlist sign-ups are sent.
#
# Deploy scripts/waitlist-sheet.gs from inside the Google Sheet
# (Extensions -> Apps Script -> Deploy -> New deployment -> Web app),
# then paste the resulting /exec URL here.
#
# Leave unset in development: submissions are logged to the console and
# discarded rather than written to the live Sheet.
VITE_WAITLIST_ENDPOINT=
