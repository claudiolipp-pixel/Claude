# Secrets only. This file is a template; copy it to .env, which stays ignored.
#
# The waitlist endpoint is NOT here — it lives in .env.production, which is
# tracked, because Vite bakes it into the shipped bundle and it is therefore
# public anyway. See README.md under "Waitlist".
